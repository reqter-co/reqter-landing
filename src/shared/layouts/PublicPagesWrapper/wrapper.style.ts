import styled from "styled-components";
import tw from "twin.macro";
// import { xs } from "@Utils/mediaQueries";

export const Wrapper = styled.div`
  ${tw`m-auto mt-56 phone:mt-40 flex flex-col w-full`};
`;
